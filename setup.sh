#!/bin/bash
# Run this script after cloning the repository to get all necessary libraries & dependencies.
# Should be updated when new dependencies are introduced
gem install rapgenius
# Note: We use Python 3
sudo pip install Naked 
